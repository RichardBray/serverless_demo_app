{
  "name": "notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.114.0"
  },
  "dependencies": {
    "@aws-cdk/core": "1.114.0",
    "@serverless-stack/cli": "^0.6.2",
    "@serverless-stack/resources": "^0.6.2",
    "aws-sdk": "^2.983.0",
    "uuid": "^7.0.3"
  }
}
